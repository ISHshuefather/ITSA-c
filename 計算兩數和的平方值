#include <stdio.h>

int main() {
    int x, y, z;
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
    // 輸入兩個整數
    scanf("%d %d", &x, &y);

    // 計算兩數之和，並輸出平方
    z = x + y;
    printf("%d\n", z * z);
    }
    return 0;
}
