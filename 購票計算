#include <stdio.h>

int main() {
    int n, n10, n5, n1;

    // 輸入金額
    scanf("%d", &n);

    // 計算各個面額的張數
    n10 = n / 10;
    n5 = (n % 10) / 5;
    n1 = n % 5;

    // 輸出結果
    printf("NT10=%d\n", n10);
    printf("NT5=%d\n", n5);
    printf("NT1=%d\n", n1);

    return 0;
}
